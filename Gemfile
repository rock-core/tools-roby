source 'https://rubygems.org'

gem 'metaruby', github: 'rock-core/tools-metaruby', branch: 'syskit2'

platform :mri do
    gem 'stackprof'
    gem 'rbtrace'
    group 'gui' do
        gem 'qtbindings'
    end
end
gem 'coveralls'

gemspec
